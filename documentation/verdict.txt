Applying the architectural decision rule ("Does the execution path vary dynamically by input intent?"), none of the 10 insurance claims force a dynamic agent loop. For all 10 claims, the required operational steps (fetch claim, inspect notes, search policy exclusions if triggered, compute payout) follow a static 4-step sequence. Over the identical 10 inputs, both systems achieved a 100.0% pass rate. However, the Fixed Workflow dominated the Agent Loop across all efficiency metrics: reducing P50 latency from 0.2793s to 0.0000s, cutting total token consumption from 6,960 to 3,834, and lowering per-claim cost from $0.001392 to $0.000767 (a 44.9% savings). Because tool calling order does not unpredictably branch across input classes, the Fixed Workflow is faster, cheaper, and 100% auditable, making it the superior architecture for this domain.
